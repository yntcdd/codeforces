#include <iostream>

using namespace std;

int main() {

    int t;
    cin >> t;

    while (t--) {
        int n, m, h;
        cin >> n >> m >> h;

        int a[n] = {};
        int b[m] = {};
        int c[m] = {};
        for (int i = 0; i < n; i++) {
            cin >> a[i];
        }
        for (int i = 0; i < m; i++) {
            cin >> b[i];
            cin >> c[i];
        }
        
        
    }

    return 0;
}